# Contributing to WSL Manager

WSL Manager への貢献をご検討いただきありがとうございます。
このドキュメントでは、開発環境のセットアップ、コーディング方針、テスト実行方法、PR 作成の流れについて説明します。

---

## 1. 開発方針・設計原則

- **標準ライブラリのみの利用**:
  - 本アプリのランタイムは Python 標準ライブラリ（GUI は `tkinter`）のみで構成されています。
  - 配布とインストールの簡易性を維持するため、ランタイムへの外部ライブラリ追加は行いません。
  - ビルド時ツール（`PyInstaller`）および品質ツール（`ruff`）のみ外部ツールを許容しています。

- **モジュールの責務分担**:
  - `wsl_core.py`: パース、検証、設定管理、スナップショット、ログ等のコアロジック（UI/winreg に依存しない純粋ロジック）。テスト可能な設計を維持します。
  - `wslmgr.py`: tkinter による GUI アプリケーション本体。
  - `wslmgr_cli.py`: argparse による CLI インターフェース。

- **データ保護・安全性**:
  - WSL ディストリビューションに対する破壊的操作（停止、登録解除、インポート/エクスポート、VHDX 圧縮、スナップショット削除等）は、実行前に確認プロンプトを経由します。

---

## 2. 環境構築とテスト手順

### 必要環境
- Python 3.10 以上 (Windows 10/11)

### 単体テストの実行
```bash
python -m unittest discover -s tests -v
```

### Lint チェック (ruff)
```bash
pip install ruff==0.15.8
ruff check .
```

### PyInstaller によるビルド確認
```bash
pip install pyinstaller
pyinstaller --clean WSLManager.spec
```

---

## 3. プルリクエスト作成の流れ

1. リポジトリをフォークし、フィーチャーブランチを作成します。
2. 変更を加え、対応する単体テストを追加・更新します。
3. `unittest` および `ruff check .` がパスすることを確認します。
4. PR テンプレートに沿って変更内容、自動テスト結果、手動確認結果を記載し、プルリクエストを送信してください。
